# React + Vite Project 🚀

This is a React project bootstrapped with [Vite](https://vitejs.dev/).  
It uses **Axios** for API requests and supports fast development with HMR.

---

## 🔧 Setup

Clone the repository:
```bash
git clone https://github.com/Hrawat01/NetflixClone.git
cd OTT_PROJECT(NETFLLIX)


Install all dependencies:
npm install
npm run dev